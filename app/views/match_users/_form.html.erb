<%= form_with(model: match_user, local: true) do |form| %>
  <% if match_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match_user.errors.count, "error") %> prohibited this match_user from being saved:</h2>

      <ul>
        <% match_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
